---
apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-channel-test
  labels:
    app: {{ template "ibm-blockchain-channel.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}-channel-test
    image: {{ .Values.image }}
    command: ["sh", "-c", "echo channel test"]
  restartPolicy: Never
