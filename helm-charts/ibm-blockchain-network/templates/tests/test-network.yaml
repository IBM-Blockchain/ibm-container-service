---
apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-network-test
  labels:
    app: {{ template "ibm-blockchain-network.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}-network-test
    image: {{ .Values.image }}
    command: ["sh", "-c", "echo network test"]
  restartPolicy: Never
